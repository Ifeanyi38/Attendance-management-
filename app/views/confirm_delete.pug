extends layout

block head
  link(rel="stylesheet", href="/static/admin_form.css")

block content
  .container.confirm-box
    h1 Confirm Delete

    p Are you sure you want to delete this record?

    .btn-row
      a.delete-btn(href=deleteUrl) Yes, delete
      a.cancel-btn(href=cancelUrl) Cancel
